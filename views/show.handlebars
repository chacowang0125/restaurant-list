<h1 class="mb-1 restaurant-show-title">{{restaurants.name}}</h1>
<div class="container">
	<div class="row">
		<div class="col-12 col-md-10 mx-auto">

			<p class="mb-1">
				<span class="text-secondary">
					<i class="fas fa-utensils pr-2"></i>
					類別：
				</span>
				{{restaurants.category}}
			</p>

			<p class="mb-1">
				<span class="text-secondary">
					<i class="fas fa-map-marker-alt pr-2"></i>
					地址：
				</span>
				{{restaurants.location}}
				<a href="{{restaurants.google_map}}" class="text-secondary" target="_blank">
					<i class="fas fa-location-arrow pr-2 fa-xs"></i>
				</a>
			</p>

			<p class="mb-1">
				<span class="text-secondary">
					<i class="fas fa-mobile-alt pr-2"></i>
					電話：
				</span>
					{{restaurants.phone}}
			</p>

			<p class="mb-5">
				{{restaurants.description}}
			</p>

			<img class="rounded mx-auto d-block mb-4 w-100"
				src="{{restaurants.image}}"
				style="max-width: 600px;">
		</div>
	</div>
	<div class="button-group text-center">
		<a href="/restaurants/{{restaurants._id}}/edit">
		<button type="submit" class="btn btn-warning btn-lg mr-3 mb-3">編輯餐廳</button>
		</a>
		<a href="/">
			<button type="submit" class="btn btn-warning btn-lg mr-3 mb-3">返回首頁</button>
		</a>
			<button type="button" class="btn btn-danger btn-lg mb-3" data-toggle="modal" data-target="#deleteModal">刪除餐廳</button>
			{{!-- Modal  --}}
		<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
			<div class="modal-dialog">
				<div class="modal-content">
					<div class="modal-header">
						<h5 class="modal-title" id="exampleModalLabel">Delete Warning</h5>
					</div>
					<div class="modal-body">
						確定要刪除餐廳「{{restaurants.name}}」嗎？
					</div>
					<div class="modal-footer">
						<button type="button" class="btn btn-secondary" data-dismiss="modal">關閉</button>
						<form action="/restaurants/{{ restaurants._id }}?_method=DELETE" method="POST" style="display: inline;">
							<button type="submit" class="btn btn-danger" data-target="#deleteModal">刪除餐廳</button>
						</form>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>